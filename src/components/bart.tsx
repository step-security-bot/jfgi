/** @format */
import { h, render, Component } from "preact";

type BartProps = {};

function Bart(props: BartProps): JSX.Element {
  return (
    <>
      <p>Bart Component</p>
    </>
  );
}

export default Bart;
